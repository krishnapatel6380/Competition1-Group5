
<!-- updatebook.ejs -->
<!DOCTYPE html>
<html lang="en">
  <!-- Pass a JavaScript variable to a partial in EJS -->
  <%- include('partials/head.ejs', { title: "Update page" }) %>
<body>
  <%- include('partials/navbar') %>


    <div class="body">
            <div class="pages">
              <div class="home">

    <div class="book-details-update">
      <h2>Edit Book-details</h2>
      <form action="/single-book/<%= book._id %>?_method=PUT" method="POST">
        <label>Title:</label>
        <input type="text" value="<%= book.title %>" id="title" name="title" required />

        <label for="author">Author:</label>
        <input type="text" value="<%= book.author %>" id="author" name="author" required /><br />

        <label for="genre">Category:</label>
        <input type="text" value="<%= book.genre %>" id="genre" name="genre" required /><br />
          
        <button type="submit">Edit Book</button>
      </form>
   
    </div>
    </div>
    </div>
  </div>


  <%- include('partials/footer') %>
</body>
</html>